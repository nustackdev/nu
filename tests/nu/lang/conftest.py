"""Lang-scoped fixtures.

These serve unit tests for ``src/nu/lang/``. Tests that need the full
compile-validate-evaluate pipeline against the finalized Nu schema belong
under ``tests/nu/integration/``, which has its own ``conftest.py``.
"""

from __future__ import annotations
