"""nu.ui python side -- component fabric.

Public entry stays at ``nu.ui`` (re-exports below). ``nu.ui.py.refs`` is the
widget kit; ``nu.ui.py.refs.base.NudleRef`` is still the widget base for the
nudle host (splitting into fabric ``Ref`` + host mixin is the next pass).
"""
