"""nudle -- dashboard app built on the nu.ui fabric.

Split from `nu.ui` in the layer/language reorg:
- `nu.nudle.py`  -- python host (serve, session, Page/Pages/Index, presets)
- `nu.nudle.ts`  -- Vite SPA + sibling PyPI wheel that ships the compiled bundle
"""
